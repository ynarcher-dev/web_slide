# 브랜드 자산

- `ynarcher-logo-horizontal.svg`: Y&ARCHER 가로형 컬러 로고. 원본 파일명은 `logo_와이앤아처_가로_컬러.svg`이다.
  - 원본 비율: 359 × 71 (viewBox 기준)
  - 사용 색상: 워드마크 `#515151`, 심볼과 강조 `#E22213`

로고는 사용자가 업로드하거나 교체하지 않는다. 코드에서는 `src/components/ui/logo.tsx`의
`Logo` 컴포넌트로만 사용하고, 경로를 화면마다 직접 적지 않는다.
