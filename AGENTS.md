# Web Slide 개발 실행 문서

> 이 문서는 Web Slide 개발을 시작하거나 이어서 수행하는 모든 작업자가 가장 먼저 읽어야 하는 실행 기준이다.  
> 제품 범위, 기술 결정, 개발 순서와 현재 진행 상태를 한 곳에서 관리한다.

## 1. 작업 시작 전 필수 절차

모든 개발 작업은 다음 순서로 시작한다.

1. 이 문서를 처음부터 끝까지 읽는다.
2. [제품 기획서](docs/product/web-slide-product-plan.md)를 읽는다.
3. [개발 스택 문서](docs/technical/development-stack.md)를 읽는다.
4. [MVP 기준 대비 보완 사항](docs/product/scope-updates.md)을 읽는다. 기획서와 처음 기준이 어디서 달라졌는지 담고 있다.
5. 아래 체크리스트에서 완료되지 않은 가장 앞 단계와 현재 작업 대상을 확인한다.
6. 저장소의 현재 코드와 변경 사항을 확인한다.
7. 이미 완료된 기능을 다시 만들지 말고, 현재 단계에서 필요한 작업을 이어서 수행한다.

문서끼리 내용이 충돌하면 우선순위는 다음과 같다.

1. 사용자의 가장 최근 명시적 지시
2. 이 `AGENTS.md`
3. 제품 기획서
4. 개발 스택 문서
5. 기존 코드의 관례

## 2. 지속 작업 프롬프트

다음 지시를 개발 작업 전체에 계속 적용한다.

> Web Slide의 개발 작업을 수행하기 전에 `AGENTS.md`, 제품 기획서와 개발 스택 문서를 읽어라. 체크리스트에서 현재 단계를 확인하고, 완료되지 않은 선행 작업을 건너뛰지 마라. 한 번의 작업에서는 안전하게 완성하고 검증할 수 있는 체크리스트 항목을 선택해 구현하라. 구현 후 관련 테스트, 타입 검사, 린트 또는 빌드를 실행하라. 실제로 구현과 검증이 끝난 항목만 `[ ]`에서 `[x]`로 변경하라. 부분 완료 항목은 체크하지 말고 필요한 경우 바로 아래에 남은 일을 하위 체크리스트로 추가하라. 작업 중 새 필수 작업, 버그 또는 기술 부채를 발견하면 적절한 단계에 새 `[ ]` 항목으로 기록하라. 매 작업이 끝날 때 반드시 이 체크리스트와 문서의 `마지막 업데이트`를 최신화하고, 사용자에게 완료 항목, 검증 결과와 다음 미완료 항목을 알려라.

## 3. 체크리스트 운영 규칙

- `[ ]`는 아직 완료되지 않은 작업이다.
- `[x]`는 구현과 필요한 검증까지 끝난 작업이다.
- 코드를 작성했더라도 검증하지 않았다면 완료 처리하지 않는다.
- 여러 기능이 들어 있는 항목은 전체 조건을 충족했을 때만 완료 처리한다.
- 일부만 끝났다면 항목을 더 작은 하위 항목으로 나누고 완료된 부분만 체크한다.
- 범위가 변경되면 기존 기록을 몰래 삭제하지 말고 항목을 수정한 이유를 `변경 기록`에 남긴다.
- 새로운 필수 작업을 발견하면 해당 개발 단계에 체크박스로 추가한다.
- 버그를 발견하고 현재 작업에서 해결하지 못했다면 `발견된 후속 작업`에 추가한다.
- 체크리스트 수정만을 목적으로 완료되지 않은 기능을 완료 처리하지 않는다.
- 단계 완료 조건을 모두 충족하기 전에는 다음 단계 전체를 완료로 간주하지 않는다.
- 매 작업 종료 시 `마지막 업데이트`의 날짜, 현재 단계와 다음 작업을 갱신한다.

## 4. 코드 작성 원칙

- 언어는 TypeScript를 사용한다.
- Next.js App Router와 React를 사용한다.
- 기본 스타일은 Tailwind CSS로 작성한다.
- 복잡한 슬라이드 스케일링과 출력 스타일에만 CSS Module 또는 별도 CSS를 사용한다.
- 부드러운 레이아웃 및 전환 애니메이션에는 Motion for React를 제한적으로 사용한다.
- DB와 인증은 Supabase를 사용한다.
- 배포 대상은 Cloudflare Workers다. `@opennextjs/cloudflare`로 변환해 올린다.
- Next.js 미들웨어(`proxy.ts`)를 두지 않는다. Node 런타임 고정이라 Workers에 올릴 수 없다.
  로그인 확인은 각 보호 페이지의 `requireUser`가, 세션 쿠키 갱신은 `SessionRefresher`가 맡는다.
- 사용자 데이터가 있는 모든 공개 스키마 테이블에 RLS를 적용한다.
- 로고는 사용자가 업로드하지 않으며 저장소의 정적 자산으로 관리한다.
- 표지는 이미지 없이 흰색 기반 배경과 tint 값만 사용한다.
- 본문 콘텐츠 영역은 16:9로 고정하며 웹페이지(iframe), 이미지, 붙여 넣은 HTML 중 하나를 넣는다.
- 세 유형은 같은 본문 레이아웃을 공유한다. 가운데 영역에 무엇을 넣는지만 다르다.
- 슬라이드 이미지는 Supabase Storage의 `slide-images` 공개 버킷에 프레젠테이션별 폴더로 저장한다.
- HTML 슬라이드는 `sandbox="allow-scripts"` iframe에서만 실행한다. `allow-same-origin`을 함께 주지 않는다.
- HTML 슬라이드에 넣을 장표의 작성 기준은 [HTML 슬라이드 작성 프롬프트](docs/product/html-slide-prompt.md)에 있다.
- 편집과 공유는 16:9를 지키되 전체화면 발표만 화면을 가로세로 100%로 채운다.
- iframe 연동 가능한 웹페이지만 사용하는 것을 제품 전제로 한다.
- MVP에 PDF 내보내기, 별도 백엔드, Redux, GraphQL, 실시간 공동 편집 또는 별도 S3를 추가하지 않는다.

## 5. 파일 크기와 분리 규칙

- 직접 작성한 `.ts`, `.tsx`, `.css` 파일은 500줄을 초과할 수 없다.
- 파일이 450줄에 도달하면 역할에 따라 분리한다.
- `page.tsx`는 화면 조립만 담당하며 200줄 이하를 권장한다.
- React 컴포넌트는 250줄 이하를 권장한다.
- URL이 다른 사용자 화면이 필요한 경우에만 새 `page.tsx`를 만든다.
- 화면 일부를 분리할 때는 컴포넌트, 훅, 데이터 접근, 액션, 타입, 상수 또는 검증 파일로 나눈다.
- 자동 생성된 Supabase 타입, lock 파일과 마이그레이션 SQL은 500줄 제한에서 제외한다.
- 코드 리뷰 시 거대한 파일, 복수 책임 컴포넌트와 중복 템플릿 렌더링을 우선 확인한다.

## 6. 공통 완료 기준

체크리스트 항목은 해당되는 다음 조건을 충족해야 완료할 수 있다.

- 요구사항대로 사용자 동작이 구현되었다.
- TypeScript 오류가 없다.
- 린트 오류가 없다.
- 관련 자동화 테스트가 통과한다.
- 중요 화면 변경은 브라우저에서 직접 확인되었다.
- 모바일 또는 좁은 편집 화면에서 치명적으로 깨지지 않는다.
- 로딩, 빈 상태와 오류 상태가 필요한 기능에는 해당 상태가 구현되었다.
- 인증과 소유권이 필요한 데이터에는 RLS 또는 서버 검증이 적용되었다.
- 새 환경 변수가 있으면 예제 환경 파일과 문서에 반영되었다.
- 새 필수 작업과 남은 문제가 아래 체크리스트에 반영되었다.

## 7. 개발 단계 및 현재 체크리스트

### 단계 0. 기획 및 기술 기준

- [x] 제품 목표와 MVP 범위를 문서화한다.
- [x] 표지와 본문 템플릿 요구사항을 확정한다.
- [x] 16:9 iframe 웹 콘텐츠 정책을 확정한다.
- [x] 슬라이드 만들기, 목록, 수정, 삭제 요구사항을 정의한다.
- [x] ~~PDF 내보내기 원칙을 정의한다.~~ 2026-08-27 범위에서 제외했다.
- [x] React, Next.js, TypeScript 기반 개발 스택을 확정한다.
- [x] Supabase DB와 최소 Storage 사용 원칙을 확정한다.
- [x] 파일당 500줄 이하 코드 규칙을 정의한다.
- [x] 개발 단계와 진행 체크리스트를 작성한다.

단계 완료 조건: 제품 기획서, 개발 스택 문서와 이 실행 문서가 존재하고 서로 모순되지 않는다.

### 단계 1. 프로젝트 기반 구성

- [x] pnpm 기반 Next.js App Router 프로젝트를 생성한다.
- [x] TypeScript strict 설정을 확인한다.
- [x] Tailwind CSS를 구성한다.
- [x] ESLint와 Prettier를 구성한다.
- [x] Motion for React를 설치한다.
- [x] Vitest와 React Testing Library를 구성한다.
- [x] Playwright E2E 환경을 구성한다.
- [x] `.env.example`과 환경 변수 검증을 추가한다.
- [x] `src` 경로 별칭을 구성한다.
- [x] 기능 단위 기본 폴더 구조를 생성한다.
- [x] 500줄 초과 파일을 탐지하는 검사 스크립트를 추가한다.
- [x] 타입 검사, 린트, 테스트와 파일 길이 검사를 하나의 검증 명령으로 묶는다.
- [x] 기본 홈 화면이 로컬에서 실행되는지 확인한다.
- [x] 초기 프로젝트 빌드가 성공하는지 확인한다.

단계 완료 조건: 새 환경에서 의존성을 설치한 후 개발 서버, 타입 검사, 린트, 테스트와 프로덕션 빌드가 모두 동작한다.

### 단계 2. 디자인 기반과 공통 UI

- [x] Y&ARCHER 로고 원본을 정적 자산으로 추가한다.
- [x] 브랜드 색상과 슬라이드 디자인 토큰을 정의한다.
- [x] Pretendard 등 기본 한글 글꼴 로딩 방식을 확정하고 적용한다.
- [x] 버튼, 입력창, 모달과 기본 메뉴 컴포넌트를 만든다.
- [x] 로딩 표시, 빈 상태와 오류 메시지 컴포넌트를 만든다.
- [x] 사용자 모션 감소 설정을 반영한다.
- [x] 키보드 포커스 스타일과 기본 접근성을 적용한다.
- [x] 편집기 기본 3단 레이아웃을 구현한다.
- [x] 좁은 화면에서 편집기 패널이 사용할 수 있게 반응형 처리를 한다.
- [x] 데이터 없이 토큰과 공통 UI를 확인하는 내부 화면 `/design-preview`를 만든다.
- [x] 공통 UI 화면의 axe 접근성 검사를 E2E에 추가한다.
- [x] 비활성 항목이 섞인 메뉴에서도 키보드 포커스가 동작하게 한다. 단계 11 키보드 점검에서 발견한 버그다.
      첫 항목이 비활성이면 포커스가 트리거에 남아 메뉴를 키보드로 쓸 수 없었다.
- [x] 내부 화면 `/design-preview`를 프로덕션 빌드에서 404로 막는다.

단계 완료 조건: 데이터 없이도 편집기의 전체 골격과 공통 UI를 확인할 수 있고 기본 접근성 검사가 통과한다.

### 단계 3. Supabase 및 인증

- [x] Supabase 로컬 개발 구성을 초기화한다.
- [x] Next.js용 브라우저 Supabase 클라이언트를 구성한다.
- [x] Next.js용 서버 Supabase 클라이언트를 구성한다.
- [x] 쿠키 기반 세션 갱신을 구성한다.
- [x] 사용자 인증 방식을 이메일 또는 매직 링크 중 하나로 확정한다. (이메일 + 비밀번호)
- [x] 로그인 화면과 동작을 구현한다.
- [x] 로그아웃을 구현한다.
- [x] 비로그인 사용자의 편집 화면 접근을 차단한다.
- [x] 미들웨어 대신 페이지별 `requireUser`로 보호 경계를 옮긴다. Cloudflare Workers가 Node 미들웨어를
      지원하지 않아 `src/proxy.ts`와 `src/lib/supabase/proxy-session.ts`를 없앴다.
- [x] 세션 쿠키 갱신을 브라우저의 `SessionRefresher`로 옮긴다. Server Component는 쿠키를 쓸 수 없다.
- [x] `profiles` 테이블 마이그레이션을 작성한다.
- [x] `presentations` 테이블 마이그레이션을 작성한다.
- [x] `slides` 테이블 마이그레이션을 작성한다.
- [x] 외래키, 정렬 컬럼과 필요한 인덱스를 추가한다.
- [x] 모든 공개 테이블에 RLS를 활성화한다.
- [x] 소유자 CRUD 정책을 작성하고 검증한다.
- [x] 공개 프레젠테이션 읽기 정책을 작성하고 검증한다.
- [x] Supabase DB TypeScript 타입 생성 명령을 추가한다.
  - [x] `pnpm db:types` 스크립트를 추가한다. 액세스 토큰과 직접 연결 두 경로를 지원한다.
  - [x] Docker로 `pnpm db:types`를 실제 실행해 `src/types/database.types.ts`를 생성 결과로 교체한다.
        손으로 쓴 파일과 컬럼, enum, 함수가 모두 일치했다. 두 번 실행해 같은 결과가 나오는 것도 확인했다.
  - [x] 생성 직후 Prettier를 돌려 `pnpm format:check`가 통과하게 한다. CLI 출력은 저장소 포맷과 다르다.
- [x] DB 타입과 애플리케이션 도메인 타입의 경계를 정의한다.
- [x] 로컬 seed 데이터를 추가한다.
- [x] 마이그레이션 적용 명령 `pnpm db:push`를 추가한다.
- [x] RLS 정책 검증 명령 `pnpm db:verify-rls`를 추가한다.
- [x] 슬라이드 이미지용 Storage 버킷과 `storage.objects` 정책을 만들고 `pnpm db:verify-rls`로 검증한다.

단계 완료 조건: 사용자가 로그인하고 자신의 데이터만 CRUD할 수 있으며, 공개 데이터의 읽기 권한이 테스트로 검증된다.

### 단계 4. 프레젠테이션 관리

- [x] 사용자 프레젠테이션 목록 화면을 구현한다.
- [x] 목록의 로딩 상태를 구현한다.
- [x] 프레젠테이션이 없을 때 빈 상태를 구현한다.
- [x] 목록 조회 실패 시 오류 상태와 재시도를 구현한다.
- [x] 새 프레젠테이션 만들기를 구현한다.
- [x] 프레젠테이션 제목 수정을 구현한다.
- [x] 프레젠테이션 삭제와 확인 절차를 구현한다.
- [x] 편집 화면으로 이동하는 동작을 구현한다.
- [x] 공통 브랜드 색상 설정을 저장한다.
- [x] 표지 배경 tint 설정을 저장한다.
- [x] 푸터 텍스트와 페이지 번호 표시 설정을 저장한다.
- [x] 공개 공유 상태를 저장한다.
- [x] 프레젠테이션 CRUD 권한 테스트를 작성한다.

단계 완료 조건: 로그인 사용자가 자신의 프레젠테이션을 만들고 목록에서 열고 수정하고 삭제할 수 있다.

### 단계 5. 슬라이드 CRUD 및 목록

- [x] 편집 화면 헤더에서 제목 수정과 공통 설정을 열 수 있게 한다. 현재 두 기능은 목록 화면에만 있다.
- [x] 편집 화면의 슬라이드 목록 조회를 구현한다.
- [x] 표지 슬라이드 만들기를 구현한다.
- [x] 본문 슬라이드 만들기를 구현한다.
- [x] 새 슬라이드를 현재 슬라이드 다음에 추가한다.
- [x] 슬라이드 선택 상태를 구현한다.
- [x] 슬라이드 목록에 페이지 번호를 표시한다.
- [x] 슬라이드 목록에 썸네일을 표시한다.
- [x] 목록에 페이지명 또는 대제목을 표시한다.
- [x] 목록에 표지와 본문 템플릿 유형을 표시한다.
- [x] 드래그를 통한 슬라이드 순서 변경을 구현한다.
- [x] 순서 변경 후 페이지 번호를 다시 계산하고 저장한다.
- [x] 슬라이드 삭제 확인을 구현한다.
- [x] 삭제 후 인접 슬라이드를 자동 선택한다.
- [x] 마지막 슬라이드 삭제 후 빈 상태를 구현한다.
- [x] 슬라이드가 없을 때 표지와 본문 만들기 동작을 제공한다.
- [x] 슬라이드 CRUD와 순서 변경 테스트를 작성한다.

단계 완료 조건: 슬라이드 생성, 목록, 선택, 순서 변경과 삭제 흐름이 저장 후 새로고침해도 유지된다.

### 단계 6. 슬라이드 템플릿 및 편집

- [x] 편집, 발표와 공유 화면에서 재사용할 공통 슬라이드 렌더러를 만든다.
- [x] 16:9 슬라이드 스케일링 컨테이너를 구현한다.
- [x] 표지 템플릿을 구현한다.
- [x] 표지 제목 입력을 구현한다.
- [x] 표지 발표자 이름 입력을 구현한다.
- [x] 표지 배경 tint 편집을 구현한다.
- [x] 본문 템플릿을 구현한다.
- [x] 본문 페이지명 입력을 구현한다.
- [x] 본문 대제목 입력을 구현한다.
- [x] 본문 소제목 입력을 구현한다.
- [x] 고정 로고를 표지와 본문 규칙에 맞게 표시한다.
- [x] 공통 푸터 텍스트를 표시한다.
- [x] 페이지 번호 표시 설정을 반영한다.
- [x] 템플릿별 속성 패널을 구현한다.
- [x] 입력 변경의 debounce 자동 저장을 구현한다.
- [x] 저장 중, 저장 완료와 저장 실패 상태를 구현한다.
- [x] 저장 실패 후 재시도 동작을 구현한다.
- [x] 템플릿 렌더러 컴포넌트 테스트를 작성한다.
- [x] 자동 저장 테스트를 작성한다.
- [x] 이미지 본문 슬라이드를 추가한다. 본문과 같은 레이아웃에 그림을 넣고, 파일은 Supabase Storage에 올린다.
- [x] 이미지 업로드, 교체, 제거와 실패 상태를 구현한다.
- [x] 슬라이드나 프레젠테이션을 지울 때 쓰지 않는 이미지 파일도 함께 지운다.
- [x] HTML 본문 슬라이드를 추가한다. 붙여 넣은 HTML을 격리된 iframe에서 보여 준다.

단계 완료 조건: 표지와 본문의 모든 필드를 편집할 수 있고 새로고침 후에도 동일한 16:9 결과가 재현된다.

### 단계 7. 웹페이지 프레임

- [x] 본문 URL 입력과 검증을 구현한다.
- [x] HTTPS URL 정책을 적용한다.
- [x] 1920×1080 기준 `WebFrame`을 구현한다.
- [x] 프레임 외곽을 16:9 비율로 유지한다.
- [x] 실제 표시 크기에 맞춘 iframe scale 계산을 구현한다.
- [x] iframe 로딩 상태를 표시한다.
- [x] 슬라이드 진입 시 새로고침 설정을 구현한다.
- [x] 편집 중 iframe 상호작용 잠금 기능을 구현한다.
- [x] 편집 미리보기에서 iframe 상호작용 활성화를 구현한다.
- [x] iframe과 편집기 포커스 전환을 검증한다.
- [x] URL 및 WebFrame 단위 테스트를 작성한다.
- [x] 실제 허용된 테스트 웹페이지로 브라우저 검증을 수행한다.
- [x] iframe을 하이드레이션 이후에 만들어 로딩 문구가 남는 문제를 없앤다. 편집, 발표, 공유 화면이
      `useMounted`로 같은 규칙을 따른다.
- [x] 웹페이지 조작을 켜면 프레임 테두리를 초록색으로 바꿔 활성 상태를 알린다. 편집 미리보기와 발표 화면이
      같은 표시를 쓴다.
- [x] 웹페이지 영역을 눌러 조작을 켜고 그 바깥을 눌러 끈다. 조작 중에는 클릭이 iframe으로 들어가므로
      끄는 자리는 웹페이지 바깥뿐이다. 키보드는 기존 버튼과 ESC를 그대로 쓴다.

단계 완료 조건: 지원 URL이 편집 화면의 고정 16:9 프레임에서 로드되고 사용자가 필요할 때 직접 조작할 수 있다.

### 단계 8. 발표 모드

- [x] 전체화면 발표 라우트를 구현한다.
- [x] 첫 슬라이드 렌더링을 구현한다.
- [x] 이전 및 다음 이동을 구현한다.
- [x] 키보드 방향키 이동을 구현한다.
- [x] 첫 페이지와 마지막 페이지의 이동 경계를 처리한다.
- [x] 현재 페이지와 전체 페이지 표시를 구현한다.
- [x] 발표 컨트롤 자동 숨김과 재표시를 구현한다.
- [x] 브라우저 전체화면 진입과 종료를 구현한다.
- [x] iframe 바깥의 발표 컨트롤을 항상 접근 가능하게 한다.
- [x] 웹페이지 조작 모드와 슬라이드 이동 모드를 구분한다.
- [x] 부드러운 슬라이드 전환 효과를 구현한다.
- [x] 빠른 연속 이동 중 전환 상태 충돌을 방지한다.
- [x] `prefers-reduced-motion`일 때 전환을 축소한다.
- [x] 발표 모드 E2E 테스트를 작성한다.
- [x] 전체화면에서는 슬라이드를 화면 가로세로 100%로 채운다. 화면 비율이 16:9가 아니면 그만큼 늘어난다.
- [x] 슬라이드 좌우 가장자리의 이동 버튼을 없앤다. 이동은 발표 컨트롤과 키보드로 한다.
- [x] 발표 컨트롤을 화면 위쪽으로 옮기고 접어 둔다. 포인터가 위쪽 120px 안에 들어오거나 키보드 포커스가
      들어오면 내려온다. 웹페이지 조작 중에는 돌아올 통로가 필요하므로 접지 않는다.
- [x] 컨트롤이 비운 자리만큼 본문 웹페이지 영역을 키운다.

단계 완료 조건: 발표자가 전체화면에서 슬라이드를 이동하고 iframe을 조작한 뒤 다시 슬라이드 이동으로 돌아올 수 있다.

### 단계 9. 공유 링크

- [x] 공개 공유 식별자 생성 방식을 구현한다. (`presentations.share_id` 기본값 `gen_random_uuid()` + `buildShareUrl`)
- [x] 읽기 전용 공유 라우트를 구현한다.
- [x] 비로그인 공개 프레젠테이션 조회를 구현한다.
- [x] 비공개 프레젠테이션 접근을 차단한다.
- [x] 공유 화면에서 편집 UI를 노출하지 않는다.
- [x] 공유 링크 복사 동작을 구현한다.
- [x] 공개 상태 변경 후 접근 권한이 즉시 반영되는지 검증한다.
- [x] 공유 권한 E2E 테스트를 작성한다.

단계 완료 조건: 공개 링크는 비로그인 상태에서 발표할 수 있고 비공개 자료는 소유자 외에 조회할 수 없다.

### 단계 10. PDF 내보내기 (범위에서 제외)

**2026-08-27 이 단계 전체를 제품 범위에서 제외했다.** 구현했던 코드와 테스트는 모두 삭제했다.
이유는 배포 대상을 Cloudflare Workers로 잡았기 때문이다. 서버가 `playwright-core`로 Chromium을
직접 띄우는 방식이라 서버리스 런타임에 올릴 수 없었다. 자세한 근거는 `변경 기록`에 있다.

삭제한 것:

- `src/features/pdf-export/` 전체
- `src/app/presentations/[presentationId]/pdf/` (전용 화면과 다운로드 라우트)
- `e2e/pdf-export.spec.ts`
- `playwright-core` 런타임 의존성
- `SlideView`의 `linkToSource` prop (PDF 화면에서만 쓰던 값)

다시 넣게 된다면 새 단계로 만들고, 서버 Chromium 없이 만드는 방법부터 정한다.

### 단계 11. 안정화 및 출시 준비

- [x] 전체 TypeScript 검사를 통과한다.
- [x] 전체 린트 검사를 통과한다.
- [x] 전체 단위 및 컴포넌트 테스트를 통과한다. (197개)
- [x] 전체 E2E 테스트를 통과한다. (chromium 21개)
- [x] 프로덕션 빌드를 통과한다.
- [x] 500줄 초과 직접 작성 파일이 없는지 확인한다. (대상 158개)
- [x] 핵심 화면의 콘솔 오류가 없는지 확인한다. (`e2e/console.spec.ts`)
- [x] Chrome, Edge, Firefox와 Safari 지원 범위를 검증한다. (`pnpm e2e:browsers`, `e2e/cross-browser.spec.ts`)
- [x] 주요 편집 동작을 키보드로 사용할 수 있는지 확인한다. (`e2e/slides.spec.ts`의 키보드 테스트)
- [x] 주요 화면의 접근성 검사를 수행한다. (로그인, `/design-preview`, 목록, 편집, 발표, 공유 6개 화면)
- [x] RLS 정책을 별도 사용자 계정으로 재검증한다. (`pnpm db:verify-rls` 22건. Storage 정책 4건 포함)
- [x] 공개 페이지에 편집 데이터나 비밀 값이 노출되지 않는지 확인한다. (`e2e/share.spec.ts`)
- [x] 환경 변수와 배포 절차를 문서화한다. ([배포 문서](docs/technical/deployment.md))
- [x] Cloudflare Workers 배포 설정을 만든다. `@opennextjs/cloudflare`, `wrangler.jsonc`,
      `open-next.config.ts`, `cf:*` 스크립트를 추가하고 Linux 컨테이너에서 번들 생성과
      `wrangler deploy --dry-run`까지 확인했다. (번들 10.7MB / gzip 2.1MB)
- [x] 터미널에서 배포하는 경로를 문서화한다. 로컬 빌드가 `.env.local`을 쓰므로 대시보드 빌드 변수가
      필요 없다. Windows 개발자 모드만 전제 조건이다.
- [x] 공유 링크를 브라우저의 실제 origin으로 만든다. 배포 도메인을 빌드 시점에 몰라도 되고,
      도메인이 바뀌어도 다시 빌드할 필요가 없다.
- [x] 대시보드에 폴더를 끌어다 놓는 경로를 만든다. `pnpm cf:dist`가 wrangler의 번들링 결과를
      `_worker.js`로 뽑아 정적 자산과 함께 `dist/`에 놓는다. Cloudflare 공식 경로가 아니며
      업로드 후 `nodejs_compat` 플래그를 넣어야 한다.
- [ ] 실제로 한 번 배포하고 결과를 확인한다. `pnpm exec wrangler login` 후
      `pnpm cf:build && pnpm cf:deploy`. 사람이 브라우저 인증을 해야 해서 대신할 수 없다.
- [x] DB 마이그레이션 적용 절차를 검증한다. (`pnpm db:push` 재실행 시 `up to date`)
- [x] 오류 모니터링 도입 여부를 결정한다. (도입하지 않음. 배포 문서 6장에 근거 기록)
- [x] README에 설치, 실행, 테스트와 빌드 방법을 작성한다.
- [x] 제품 기획서와 개발 스택 문서를 현재 구현에 맞춘다. 슬라이드 유형 4종과 발표 리모컨 변경이
      두 문서에 반영되지 않아 실제 화면과 어긋나 있었다.
- [x] 기준 대비 달라진 점을 [보완 사항 문서](docs/product/scope-updates.md)로 남긴다.
- [ ] MVP 수동 사용자 시나리오를 처음부터 끝까지 수행한다.
  - [x] 사람이 따라갈 확인 절차를 [수동 확인 시나리오](docs/technical/mvp-manual-scenario.md)로 작성한다.
  - [ ] 프로덕션 빌드를 띄우고 그 문서를 처음부터 끝까지 실제로 수행한다. 자동화 검증은 모두 끝났고
        사람이 직접 훑는 확인만 남았다. 수행 결과는 문서 끝의 기록 표에 남긴다.

단계 완료 조건: 새 환경에서 프로젝트를 설치하고 배포할 수 있으며 핵심 사용자 시나리오와 보안 경계가 검증된다.

## 8. 발견된 후속 작업

작업 중 발견했지만 즉시 처리하지 않은 필수 작업을 여기에 추가한다.

- [ ] 새로 받은 저장소에서 `pnpm validate`가 바로 실패한다. Next.js 16이 `PageProps`와 `LayoutProps`를
      `.next/types`에 생성하므로 `pnpm build`를 한 번 돌린 뒤에야 `pnpm typecheck`가 통과한다.
      `validate` 앞에 타입 생성 단계를 넣을지 정한다. CI를 붙이기 전에 반드시 처리해야 한다.
- [ ] `pnpm validate`에 E2E를 포함할지 결정한다. 현재는 `pnpm e2e`로만 실행된다. E2E가 원격 DB와 외부 네트워크를 쓰므로 아직 묶지 않았다.
- [ ] 대화로 공유된 Supabase DB 비밀번호를 교체하고 `.env.local`을 갱신한다.
- [ ] E2E가 원격 DB의 seed 계정에 의존한다. CI에서 쓰려면 전용 테스트 프로젝트 또는 로컬 Supabase가 필요하다.
- [ ] 비밀번호 재설정 화면을 만들지 결정한다. 현재는 로그인과 회원가입만 있다.
- [ ] `cn`이 Tailwind 클래스 충돌을 합치지 않는다. 공통 UI에 폭이나 여백을 밖에서 덮어쓰는 요구가 늘어나면 `tailwind-merge` 도입을 검토한다.
- [ ] `e2e/web-frame.spec.ts`가 외부 사이트 `https://example.com`을 iframe으로 띄운다. 네트워크가 없는 CI에서는 실패하므로 사내 테스트 페이지로 바꿀지 결정한다.
- [ ] `e2e/cross-browser.spec.ts`의 `msedge` 프로젝트는 시스템에 설치된 Edge를 쓴다. Edge가 없는 CI에서는 이 프로젝트만 빼야 한다.
- [ ] 발표 화면은 슬라이드를 모두 DOM에 두고 현재 슬라이드 ±1만 iframe으로 띄운다. 슬라이드가 20장을 넘는 경우의 메모리와 로딩 시간을 측정한다.
- [ ] 표지 배경 tint는 프레젠테이션 공통 설정이라 표지 속성 패널에서 바꾸면 모든 표지에 적용된다. 슬라이드별 tint가 필요한지 확인한다.
- [ ] 공유 링크를 회수하는 방법(`share_id` 재발급)이 없다. 필요한지 결정한다.
- [ ] 슬라이드 이미지 버킷이 공개다. 파일명이 무작위라 주소를 추측할 수는 없지만, 주소를 아는 사람은
      비공개 자료의 이미지도 볼 수 있다. 비공개 버킷과 서명 URL로 바꿀지 결정한다. 공유 화면이
      로그인 없이 이미지를 읽어야 하는 점이 걸림돌이다.
- [ ] 이미지 정리가 실패해도 삭제는 계속 진행한다. 쓰이지 않는 파일이 쌓이는지 주기적으로 확인하고,
      필요하면 정리 작업을 따로 만든다.
- [ ] HTML 슬라이드는 붙여 넣은 코드를 검사하지 않는다. 샌드박스가 우리 세션을 지켜 주지만 바깥으로
      네트워크 요청은 나갈 수 있다. 사내 사용 범위를 넘어가면 허용 태그 검사를 넣을지 결정한다.
- [ ] E2E가 실제 Supabase Storage에 파일을 올린다. 테스트가 중간에 실패하면 파일이 남으므로
      전용 테스트 프로젝트를 쓸지 결정한다.
- [ ] 발표 컨트롤이 접혀 있어 처음 쓰는 사람이 못 찾을 수 있다. 발표 시작 후 잠시 보여 준 뒤 접는 것으로
      충분한지, 상단에 얇은 손잡이 표시가 필요한지 실제 사용 후 결정한다.
- [ ] 전체화면 발표는 화면 비율이 16:9가 아니면 슬라이드를 늘려서 채운다. 16:10이나 21:9 화면에서 글자 왜곡이
      실제로 거슬리는지 확인하고, 필요하면 채우기와 여백 유지 중 고를 수 있게 할지 결정한다.
- [ ] `pnpm cf:dist`로 만든 `dist/`가 대시보드 업로드에서 실제로 동작하는지 확인되지 않았다.
      Cloudflare 문서에 이 흐름의 `_worker.js` 지원 여부가 없다. 한 번 시도해 보고
      안 되면 이 경로와 스크립트를 지운다.
- [ ] `NEXT_PUBLIC_SITE_URL`은 이제 서버 렌더 결과의 대비값일 뿐이다. 쓰는 곳이 더 늘지 않는다면
      아예 없앨지 정한다.
- [ ] 자바스크립트를 끈 브라우저에서는 세션 쿠키가 갱신되지 않는다. 미들웨어를 쓸 수 없어 생긴 제약이며,
      실제로 문제가 되는지 확인한 뒤 서버 액션 기반 갱신을 넣을지 정한다.
- [ ] 배포 명령이 `npx wrangler versions upload`라 새 버전만 올라가고 트래픽은 넘어가지 않는다.
      `npx wrangler deploy`로 바꿀지, 대시보드에서 수동으로 넘길지 정한다.
- [ ] 루트 `/`가 제품 이름만 보여 주는 자리 표시 화면이다. `/presentations`로 보낼지, 로그인 링크가 있는 랜딩으로 만들지 결정한다.

## 9. 현재 상태

- 마지막 업데이트: 2026-08-27
- 현재 단계: 단계 11 진행 중 — Cloudflare 대시보드 설정과 사람이 직접 훑는 MVP 수동 시나리오가 남음. 단계 10(PDF)은 범위에서 제외
- 마지막 완료 작업: 대시보드 업로드용 `pnpm cf:dist` 추가. wrangler가 배포 시점에 하던 번들링을 미리 돌려 `_worker.js` 한 파일로 뽑고 정적 자산과 함께 `dist/`에 놓는다. 검증되지 않은 경로이며 `pnpm cf:deploy`가 여전히 확실한 방법이다
- 그 전 완료 작업: 공유 링크를 브라우저의 실제 origin으로 만들도록 변경. 배포 도메인을 빌드 시점에 몰라도 되고 도메인이 바뀌어도 다시 빌드할 필요가 없다. `NEXT_PUBLIC_SITE_URL`은 서버 렌더 결과의 대비값으로 내려갔다
- 그 전 완료 작업: Cloudflare Workers 배포 설정. `@opennextjs/cloudflare` 어댑터와 `wrangler.jsonc`를 추가하고, Node 미들웨어를 쓸 수 없어 `src/proxy.ts`를 없애고 보호 경계를 페이지별 `requireUser`로, 세션 갱신을 브라우저 `SessionRefresher`로 옮겼다. Next는 어댑터 요구에 맞춰 16.3.3으로 올렸다
- 그 전 완료 작업: PDF 내보내기 기능 전체 삭제. Cloudflare Workers 배포가 서버 Chromium을 띄울 수 없어 범위에서 뺐다. 코드, 라우트, 테스트, `playwright-core` 의존성과 관련 문서를 모두 정리했다
- 그 전 완료 작업: 남은 미완료 항목 정리와 문서 동기화. `pnpm db:types`를 Docker로 실제 실행해 손으로 쓴 DB 타입을 생성 결과로 교체하고, 슬라이드 유형 4종과 발표 리모컨 변경을 제품 기획서·개발 스택 문서에 반영했다. 기준 대비 달라진 점은 [보완 사항 문서](docs/product/scope-updates.md)에, 사람이 훑을 절차는 [수동 확인 시나리오](docs/technical/mvp-manual-scenario.md)에 남겼다
- 그 전 완료 작업: 슬라이드 유형 2종 추가. 이미지 슬라이드(Supabase Storage 업로드, 소유자 RLS, 삭제 시 정리)와 HTML 슬라이드(격리된 iframe 뷰어)를 본문과 같은 레이아웃으로 구현
- 그 전 완료 작업: 발표 화면 개선 요청 반영. 전체화면 가로세로 100% 채우기, 좌우 가장자리 이동 버튼 제거, 조작 중 초록 테두리, 화면을 눌러 조작을 켜고 끄기, 발표 컨트롤을 상단 hover 표시로 옮기고 그만큼 웹페이지 영역 확대
- 검증 상태: `pnpm validate` 통과(타입 검사, 린트, 포맷, 단위 테스트 197개, 파일 길이 검사 160개), chromium E2E 21개 통과(인증 6건 포함), Linux 컨테이너에서 `pnpm cf:build`와 `wrangler deploy --dry-run` 통과, `pnpm db:verify-rls` 22/22 통과(Storage 정책 4건 포함), 프로덕션 빌드 통과. `pnpm db:types`를 두 번 실행해 같은 결과가 나오는 것과 `pnpm format:check`를 깨지 않는 것을 확인. `pnpm e2e:browsers` 3건(firefox·webkit·msedge)은 직전 실행 결과를 유지
- 다음 권장 작업: `pnpm exec wrangler login` 후 `pnpm cf:deploy`로 한 번 배포한다. 번들은 이미 만들어져 있고, 배포 도메인은 몰라도 된다 그다음 [수동 확인 시나리오](docs/technical/mvp-manual-scenario.md)를 배포된 화면에서 사람이 한 번 수행하고 문서 끝의 기록 표를 채운다
- 현재 알려진 차단 요소: 터미널 배포에는 Windows 개발자 모드와 `wrangler login`이 필요하며 둘 다 사람이 직접 해야 한다. 대시보드 빌드를 쓴다면 빌드 명령과 `NEXT_PUBLIC_*` 빌드 변수를 등록해야 한다. 그 밖에 `pnpm db:types`는 Docker 데몬이 떠 있어야 하고(`SUPABASE_ACCESS_TOKEN`이 있으면 Docker 없이 가능), E2E를 돌리려면 새 환경에서 `pnpm exec playwright install chromium`을, `pnpm e2e:browsers`를 쓰려면 `pnpm exec playwright install firefox webkit`을 먼저 실행해야 한다

## 10. 변경 기록

중요한 범위 변경과 체크리스트 구조 변경만 기록한다. 개별 코드 변경 내역은 Git 기록을 사용한다.

| 날짜       | 변경 내용                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-26 | 최초 개발 단계, 진행 체크리스트와 지속 작업 프롬프트 작성                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-26 | 단계 2에 내부 확인 화면 `/design-preview`와 axe 접근성 E2E 항목 추가. 단계 완료 조건인 "데이터 없이 확인"과 "기본 접근성 검사"를 검증 가능한 형태로 만들기 위함                                                                                                                                                                                                                                                       |
| 2026-08-26 | 인증 방식을 이메일 + 비밀번호로 확정. 매직 링크는 메일 발송에 의존해 로그인 흐름을 자동으로 검증하기 어려워 제외                                                                                                                                                                                                                                                                                                      |
| 2026-08-26 | 단계 3에 `pnpm db:push`와 `pnpm db:verify-rls` 항목 추가. RLS 정책을 실제로 검증하려면 실행 가능한 명령이 필요함                                                                                                                                                                                                                                                                                                      |
| 2026-08-26 | DB 타입 생성 항목을 하위 항목으로 분리. 명령은 추가했지만 Docker와 액세스 토큰이 없어 실제 생성까지 확인하지 못함                                                                                                                                                                                                                                                                                                     |
| 2026-08-26 | 새 프레젠테이션 만들기를 개발 스택 문서의 `/presentations/new` 라우트 대신 목록 화면의 모달로 구현. 제목 하나만 받는 화면이라 별도 URL을 둘 이유가 없음                                                                                                                                                                                                                                                               |
| 2026-08-26 | 단계 4에 목록 오류 상태 항목을 추가. 공통 완료 기준의 "오류 상태 구현"을 목록 화면에서 검증 가능한 형태로 만들기 위함                                                                                                                                                                                                                                                                                                 |
| 2026-08-26 | 공통 슬라이드 렌더러를 `src/features/slide-renderer`로 분리. 개발 스택 문서의 폴더 목록에는 없지만 편집 미리보기, 썸네일, 발표, PDF가 같은 컴포넌트를 재사용해야 해서 별도 기능으로 둠                                                                                                                                                                                                                                |
| 2026-08-26 | 슬라이드 순서 변경을 `reorder_slides` DB 함수로 처리. 행마다 update를 보내면 중간 순서가 화면에 보일 수 있어 한 번의 요청으로 묶음                                                                                                                                                                                                                                                                                    |
| 2026-08-26 | 발표 화면은 슬라이드를 모두 마운트한 채 현재 것만 보여 주는 방식으로 구현. 이동할 때마다 iframe을 다시 만들면 시연 중이던 웹페이지 상태가 사라지기 때문                                                                                                                                                                                                                                                               |
| 2026-08-26 | 저장 상태 표시를 편집 헤더 한 곳으로 통일. 헤더와 속성 패널에 같은 문구가 두 번 보여 혼란스러웠음                                                                                                                                                                                                                                                                                                                     |
| 2026-08-26 | 공유 화면을 발표 화면 컴포넌트 재사용으로 구현. `exitHref`를 넘기지 않으면 편집으로 돌아가는 링크가 사라지므로 화면을 두 벌 만들 이유가 없음                                                                                                                                                                                                                                                                          |
| 2026-08-26 | PDF 다운로드를 `/api` 대신 `/presentations/[id]/pdf/download` 라우트 핸들러로 배치. proxy의 보호 경로 안에 들어가 로그인 확인이 한 겹 더 걸림                                                                                                                                                                                                                                                                         |
| 2026-08-26 | PDF 화면은 하이드레이션이 끝난 뒤에 슬라이드를 만든다. 서버 HTML에 iframe이 들어 있으면 로딩 완료 이벤트를 놓쳐 다 불러온 웹페이지를 계속 기다림                                                                                                                                                                                                                                                                      |
| 2026-08-26 | 웹페이지를 제한 시간 안에 못 불러오면 PDF를 실패 처리. 반쯤 빈 PDF를 내려주는 대신 원인을 알리라는 개발 스택 문서 4장 원칙을 따름                                                                                                                                                                                                                                                                                     |
| 2026-08-26 | 내부 확인용 `/design-preview`를 지우지 않고 프로덕션 빌드에서만 404 처리. 공통 UI를 데이터 없이 확인하고 접근성 검사를 돌릴 곳이 계속 필요함                                                                                                                                                                                                                                                                          |
| 2026-08-26 | 오류 모니터링 서비스를 MVP에 넣지 않기로 결정. 슬라이드 제목과 웹페이지 주소가 외부로 나가고, 현재 규모에서는 서버 로그와 Supabase 로그로 추적 가능. 근거는 배포 문서 6장                                                                                                                                                                                                                                             |
| 2026-08-26 | 브라우저 지원 검증을 전체 E2E 대신 `e2e/cross-browser.spec.ts` 스모크로 분리. 전체 스위트를 네 엔진에서 돌리면 실행 시간이 길고, 엔진 차이를 볼 지점은 핵심 경로로 충분함                                                                                                                                                                                                                                             |
| 2026-08-26 | E2E의 로그인·생성·삭제 절차를 `e2e/helpers.ts`로 모음. 같은 코드가 다섯 파일에 복사돼 있어 화면이 바뀔 때마다 다섯 곳을 고쳐야 했음                                                                                                                                                                                                                                                                                   |
| 2026-08-26 | 메뉴가 비활성 항목을 건너뛰도록 수정. 단계 11 키보드 점검에서 발견한 실제 버그로, 슬라이드가 한 장일 때 메뉴를 키보드로 쓸 수 없었음                                                                                                                                                                                                                                                                                  |
| 2026-08-27 | 전체화면 발표에서 16:9 잠금을 풀고 화면을 가로세로 100%로 채우도록 변경. 사용자 요청이며, 화면 비율이 16:9가 아니면 슬라이드가 늘어나는 대신 검은 여백이 사라짐                                                                                                                                                                                                                                                       |
| 2026-08-27 | 발표 화면 좌우 가장자리의 이동 버튼 제거. 사용자 요청이며 이동은 아래 발표 컨트롤과 키보드 방향키로 그대로 가능                                                                                                                                                                                                                                                                                                       |
| 2026-08-27 | 웹페이지 조작 중 프레임 테두리를 초록색으로 표시. 조작 가능 여부를 버튼 문구 말고 슬라이드에서도 알 수 있게 함                                                                                                                                                                                                                                                                                                        |
| 2026-08-27 | 웹페이지 조작을 화면 터치로 전환. 덮개는 포커스를 받지 않는 클릭 영역으로 두고 키보드는 기존 버튼을 쓴다. 슬라이드가 `role="img"`라 안쪽에 버튼을 두면 접근성 검사(nested-interactive)에 걸림                                                                                                                                                                                                                         |
| 2026-08-27 | 발표 컨트롤을 하단 상시 표시에서 상단 hover 표시로 이동. 접힌 동안 슬라이드 클릭을 막지 않도록 층은 클릭을 받지 않고 패널만 받는다. E2E는 `revealPlayerControls`로 먼저 내려 놓고 조작한다                                                                                                                                                                                                                            |
| 2026-08-27 | 본문 슬라이드 여백을 줄여 웹페이지 영역을 키움. 프레임 높이가 가로 폭을 정하므로 위아래 여백이 가장 크게 먹힘                                                                                                                                                                                                                                                                                                         |
| 2026-08-27 | 본문 슬라이드에 이미지 유형 추가. 템플릿 값을 늘려 목록과 메뉴에서 유형을 구분하고, 콘텐츠 영역만 갈라 본문 레이아웃은 그대로 재사용                                                                                                                                                                                                                                                                                  |
| 2026-08-27 | 슬라이드 이미지를 Supabase Storage 공개 버킷에 저장. 공유 화면과 서버 PDF가 로그인 없이 읽어야 해서이며, 쓰기는 소유자 RLS로 제한하고 파일명은 무작위로 만든다                                                                                                                                                                                                                                                        |
| 2026-08-27 | 붙여 넣은 HTML을 보여 주는 HTML 슬라이드 추가. `sandbox="allow-scripts"`로 같은 출처를 주지 않아 서비스 세션과 분리한다                                                                                                                                                                                                                                                                                               |
| 2026-08-27 | `pnpm db:types`를 Docker로 실제 실행해 손으로 쓴 `database.types.ts`를 생성 결과로 교체. 컬럼, enum, 두 함수가 모두 일치해 스키마 기준이 확인됨. 생성 직후 Prettier를 돌려 재생성이 `pnpm format:check`를 깨지 않게 함                                                                                                                                                                                                |
| 2026-08-27 | 슬라이드 유형 추가와 발표 리모컨 변경을 제품 기획서와 개발 스택 문서에 반영. 두 문서가 여전히 "본문은 웹페이지뿐", "이미지 업로드 없음", "화면 가장자리 이동 버튼"을 말하고 있어 실제 화면과 어긋났음                                                                                                                                                                                                                 |
| 2026-08-27 | 기준 대비 달라진 점만 모은 [보완 사항 문서](docs/product/scope-updates.md)를 추가. 기획서와 스택 문서는 항상 현재 제품을 설명하므로 무엇이 왜 바뀌었는지가 남지 않았음                                                                                                                                                                                                                                                |
| 2026-08-27 | 터미널 배포 경로를 기본으로 문서화. 로컬 빌드가 `.env.local`을 쓰므로 대시보드 빌드 변수가 필요 없다                                                                                                                                                                                                                                                                                                                  |
| 2026-08-27 | 공유 링크를 `NEXT_PUBLIC_SITE_URL` 대신 **브라우저의 실제 origin**으로 만들도록 변경. 배포 도메인을 모르는 채로 배포해도 링크가 맞고, 도메인이 바뀌어도 다시 빌드하지 않아도 된다. 다이얼로그는 클라이언트 컴포넌트라 마운트 이후 `window.location.origin`을 쓰고, 환경 변수는 하이드레이션 전 서버 렌더 결과의 대비값으로만 남는다                                                                                   |
| 2026-08-27 | 대시보드에 폴더를 끌어다 놓는 배포 경로를 `pnpm cf:dist`로 추가. `.open-next/worker.js`는 4KB짜리 진입점일 뿐이고 실제 코드는 51MB 트리에 흩어져 있어 그대로 올리면 서버 코드가 돌지 않는다. `wrangler deploy --dry-run --outdir`로 번들링 결과만 뽑아 `_worker.js`로 놓는다. Cloudflare가 안내하는 경로가 아니고 `nodejs_compat` 플래그를 따로 넣어야 해서, 확실한 방법은 여전히 `pnpm cf:deploy`다                  |
| 2026-08-27 | 배포 대상을 **Cloudflare Workers**로 확정하고 `@opennextjs/cloudflare` 어댑터, `wrangler.jsonc`, `open-next.config.ts`, `cf:*` 스크립트를 추가. 증분 캐시(R2/KV)와 이미지 바인딩은 두지 않았다. 데이터 화면이 모두 동적 라우트이고 로고가 `unoptimized`라 쓸 일이 없다                                                                                                                                                |
| 2026-08-27 | **`src/proxy.ts`와 `src/lib/supabase/proxy-session.ts`를 삭제하고 인증 경계를 페이지로 옮김.** Next.js 16의 `proxy.ts`는 Node 런타임 고정이라(`runtime` 옵션은 오류) `@opennextjs/cloudflare`가 빌드 단계에서 거부한다. 보호 화면은 `requireUser`가 막고, 로그인 화면은 `getCurrentUser`로 우회시키며, 세션 쿠키 갱신은 보호 레이아웃의 `SessionRefresher`가 브라우저에서 맡는다. 기존 E2E 인증 6건이 그대로 통과한다 |
| 2026-08-27 | Next.js를 16.3.2에서 16.3.3으로 올림. `@opennextjs/cloudflare` 최신 버전의 peer 조건이 `>=16.3.3`이라 어댑터를 따라갈 수 있게 맞췄다. 어댑터 자체는 pnpm의 최소 배포 경과 정책을 지키는 1.20.2를 쓴다                                                                                                                                                                                                                 |
| 2026-08-27 | **PDF 내보내기 기능을 제품 범위에서 제외하고 관련 코드·테스트·문서를 모두 삭제.** 배포 대상을 Cloudflare Workers로 정했는데, 서버가 `playwright-core`로 Chromium을 직접 띄우는 구조라 서버리스 런타임에 올릴 수 없었다. 남은 선택지는 Browser Rendering 바인딩으로 재작성하거나 별도 워커로 분리하는 것인데 둘 다 MVP 범위를 넘는다. 단계 10 전체와 제품 기획서 9장, 개발 스택 문서 4장이 함께 빠졌다                 |
| 2026-08-27 | 단계 11의 마지막 항목을 문서 작성과 실제 수행으로 나누고 [수동 확인 시나리오](docs/technical/mvp-manual-scenario.md)를 추가. "처음부터 끝까지 수행한다"만으로는 무엇을 봐야 하는지가 사람마다 달라짐                                                                                                                                                                                                                  |

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
